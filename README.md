# Telegram Challenge Bot

Бот для тестирования различных возможностей Telegram Bot API.

## Изученные возможности

### Базовые функции
- ✅ Написание сообщений в группе
- ✅ Закрепление сообщений (требуются права администратора)
- ✅ Изменение закрепленных сообщений (только свои сообщения, в течение 48 часов)
- ✅ Удаление сообщений (свои - 48 часов, админ - без ограничений)

### Типы кнопок
- ✅ **Inline кнопки с callback_data** - кнопки под сообщением, отправляют callback
- ✅ **Inline кнопки с URL** - открывают ссылки в браузере
- ✅ **Reply клавиатура** - кнопки рядом с полем ввода (заменяют обычную клавиатуру)
- ✅ **Удаление сообщения после нажатия кнопки** - обработка callback + удаление сообщения

### Уведомления
- ✅ **callback.answer()** - всплывающие уведомления (toast) при нажатии на кнопку
  - `show_alert=False` - короткое уведомление внизу экрана
  - `show_alert=True` - модальное окно с кнопкой OK

### Меню бота
- ✅ **Bot Commands Menu** - кнопка рядом с вложениями (📎), работает везде
- ✅ **Bot Menu Button** - кнопка рядом с эмодзи (😀), только в личных чатах

## Структура проекта

- `main.py` - основной файл бота (чистый, для реального функционала)
- `test_commands.py` - тестовые команды для изучения возможностей
- `requirements.txt` - зависимости проекта

## Запуск

1. Создай виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate
```

2. Установи зависимости:
```bash
pip install -r requirements.txt
```

3. Создай `.env` файл в родительской директории с токеном:
```
BOT_CHALLENGE_TOKEN=твой_токен_от_BotFather
```

4. Запусти бота:
```bash
python main.py
```

## Технологии

- Python 3.10+
- aiogram 3.13.1
- python-dotenv 1.0.1

